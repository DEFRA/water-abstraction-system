{% extends 'layout-licences.njk' %}

{% from "govuk/components/pagination/macro.njk" import govukPagination %}

{% from "macros/contacts-table.njk" import contactsTable %}

{% block subPageContent %}
  {% if customerContactLink %}
    <p class="govuk-body"><a class="govuk-link" href={{ customerContactLink }}>Go to licence holder contacts</a></p>
  {% endif %}

  {% if contacts.length === 0 %}
    <p>No licence contacts found.</p>

  {% else %}
    {{ contactsTable(contacts, pagination) }}
  {% endif %}

  {% if pagination.numberOfPages > 1 %}
    {{ govukPagination(pagination.component) }}
  {% endif %}
{% endblock %}
