{% macro pageHeading(licenceReference, pageTitle, classes) %}
  <span class="govuk-caption-l">Licence {{ licenceReference }}</span>
  <h1 class="govuk-form-group {{ classes if classes else 'govuk-heading-l' }}">
    {{ pageTitle }}
  </h1>
{% endmacro %}
