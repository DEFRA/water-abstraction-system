'use strict'

/**
 * Formats data for the `/notices/setup/{sessionId}/abstraction-alert/cancel` page
 * @module CancelAlertsPresenter
 */

/**
 * Formats data for the `/notices/setup/{sessionId}/abstraction-alert/cancel` page
 *
 * @returns {object} - The data formatted for the view template
 */
function go() {
  return {}
}

module.exports = {
  go
}
