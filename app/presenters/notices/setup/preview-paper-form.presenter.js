'use strict'

/**
 * Formats data for the `notices/setup/{sessionId}/preview-paper-form` page
 * @module PreviewPaperFormPresenter
 */

/**
 * Formats data for the `notices/setup/{sessionId}/preview-paper-form` page
 *
 * @returns {object} - The data formatted for the view template
 */
function go() {
  return {}
}

module.exports = {
  go
}
